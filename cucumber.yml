---

    default: '--p pretty -p html'
    json: '--p pretty -p json '
    
    pretty: --format pretty
    progress: --format progress
    html: --format html --out=reports/marvel.html
    json: --format json --out reports/marvel.json